---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: traefik

resources:
  - namespace.yaml
  - netpol.yaml
  - rbac.yaml
  - sync.yaml
  - whoami.yaml

patches:
  - path: middlewares.yaml
