---
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: prometheus-blackbox-exporter
  namespace: infra-observability
spec:
  interval: 1h30m
  layerSelector:
    mediaType: "application/vnd.cncf.helm.chart.content.v1.tar+gzip"
    operation: copy
  url: oci://ghcr.io/prometheus-community/charts/prometheus-blackbox-exporter
  ref:
    tag: "11.17.2"
    digest: "sha256:e79a38923ad8dc167cae22b94c3ea0a251eb82e9034e6745dd6f534cd46b00e9"
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: prometheus-blackbox-exporter
  namespace: infra-observability
spec:
  interval: 10m0s
  driftDetection:
    mode: enabled
  chartRef:
    kind: OCIRepository
    name: prometheus-blackbox-exporter

  values:

    config:
      modules:
        http_2xx:
          prober: http
          timeout: 30s
          http:
            method: GET
            follow_redirects: true
        http_4xx:
          prober: http
          timeout: 30s
          http:
            method: GET
            valid_status_codes: [401, 404]
            follow_redirects: true

    prometheusRule:
      enabled: true
      namespace: infra-observability
      rules:
        - alert: BlackboxProbeFailed
          expr: 'probe_success == 0'
          for: 15m
          labels:
            severity: critical
          annotations:
            summary: >-
              Blackbox probe failed (instance {{ index $labels "instance" }})
            description: |
              Blackbox exporter failed to probe {{ index $labels "instance" }}

    serviceMonitor:
      enabled: true
      defaults:
        module: http_2xx
        interval: 2m30s
      selfMonitor:
        enabled: true